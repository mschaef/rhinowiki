Message Dialog

<a href="http://www.sliver.com/dotnet/MessageDialog/">This</a> be should part of the Win32 API if it's not already. 
Basically, it amounts to a variant of the MessageBox API that allows custom button labels, rather than just "Yes", 
"No", "Abort", "Retry", etc.

<br><br>

Maybe this kind of API would have made abominations like this less likely:

<br><br>

<img src="http://digilander.libero.it/chiediloapippo/Engineering/iarchitect/vbsys1.gif"
     height="119" width="366">

<br><br>

At least users might be able to avoid calling errors "OK", of all things...
